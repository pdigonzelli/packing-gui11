create database "plus/plus91b".
connect -1 -ldb plus -db plus/plus91b.db.
/*
CREATE TABLE fileCfg (
  fcID      CHARACTER(15) NOT NULL UNIQUE,
  fcUpdated CHARACTER(15).
CREATE TABLE propCfg (
  pcID      CHARACTER(15) NOT NULL UNIQUE,
  pcName    CHARACTER(15),
  pcValue   CHARACTER(30).
CREATE TABLE amCfg (
  acID      CHARACTER(15),
  acValue   CHARACTER(30).
*/
disconnect plus.
